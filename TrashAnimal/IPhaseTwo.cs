namespace TrashAnimal;

/// <summary>Placeholder for Phase 2 resolution (tokens, actions, scoring).</summary>
public interface IPhaseTwo
{
    void ResolvePhaseTwo(int playerIndex, IReadOnlyList<TokenAction> tokens);
}
